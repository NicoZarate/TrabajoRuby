


<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
        <h1>Crear </h1>
    </div>
  </div>
</nav>
  
<div class="container-fluid text-center">    
  <div class="row content">
    <div class="col-sm-2 sidenav">
         <ul class="nav nav-pills nav-stacked">
            <li><%= link_to 'Back', list_path(@list) %>
          </ul>  
    </div>
    <div class="col-sm-8 text-left"> 
       <h2>Tarea Larga</h2>
     <%= form_for @longTask, url: {:action => :create} do |f| %>
             <% if @longTask.errors.any? %>
              <div id="error_explanation">
                <h2>
                  <%= pluralize(@longTask.errors.count, "error") %> prohibited
                  this longTask from being saved:
                </h2>
                <ul>
                  <% @longTask.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                  <% end %>
                </ul>
              </div>
            <% end %>
              <p>
              <%= f.label :descripción %><br>
              <%= f.text_field :description %>
            </p>
            <p>
              <%= f.label :estado %><br>
              <%= f.select :state, ['Pediente','curso', 'Hecha'] %><br>
              
            </p>
            <p>
              <%= f.label :prioridad %><br>
              <%= f.select :priority, ['alta', 'media','baja'] %>
            </p>
            <p>
              <%= f.label :porcentaje_de_avance %><br>
              <%= f.select :percentage, (0..100) %>%
            </p>
            <%= f.submit :enviar, :class => "btn btn-primary" %>
          </p>
     <% end %>
    
  </div>
</div>