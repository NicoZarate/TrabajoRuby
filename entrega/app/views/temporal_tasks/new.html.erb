<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
        <h1>Crear </h1>
    </div>
  </div>
</nav>
  
<div class="container-fluid text-center">    
  <div class="row content">
    <div class="col-sm-2 sidenav">
         <ul class="nav nav-pills nav-stacked">
            <li><%= link_to 'Back', lists_path %>
          </ul>  
    </div>
    <div class="col-sm-8 text-left"> 
      <h2>tasks</h2>
     <div class="form-inputs">  
      <%= form_for @temporalTask, url: { :action => :create } do |f| %>
        <% if @temporalTask.errors.any? %>
              <div id="error_explanation">
                <h2>
                  <%= pluralize(@temporalTask.errors.count, "error") %> prohibited
                  this temporalTask from being saved:
                </h2>
                <ul>
                  <% @temporalTask.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                  <% end %>
                </ul>
              </div>
            <% end %>

          <p>
            <%= f.label :descripciòn %><br>
            <%= f.text_field :description %>
          </p>
          <p>
            <%= f.label :estado %><br>
            <%= f.select :state, ['Pediente', 'Hecha'] %><br>
            
          </p>
          <p>
            <%= f.label :prioridad %><br>
            <%= f.select :priority, ['alta', 'media','baja'] %>
          </p>
          <p>
            <%= f.label :inicio %><br>
             <%= f.date_select :validation_start %>
          </p>
          <p>
            <%= f.label :fin %><br>
             <%= f.date_select :validation_end %>
          </p>
          <p>
            <%= f.submit :"enviar" %>
          </p>
    <% end %>
    </div>
    
  </div>
</div>